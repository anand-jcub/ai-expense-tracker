# Change Contract: Monthly Trend Chart Line Representation

## Intent
Update the Monthly Trend Chart (`#monthlyTrendChart`) on the dashboard from a bar chart to a smooth, modern line chart with area fill and points.

## Zones Touched
- **Zone P / UI Edge**: `expense_tracker/static/app.js` (Chart.js monthly trend chart renderer: change type to `'line'`, configure line stroke, smooth curve tension, points, and area fill)

## Shared Concepts
- Data attributes (`data-labels-expenses`, `data-values-expenses`, etc.) generated by `templates.py` remain identical.
- Active card switcher (`credits`, `debits`, `expenses`) toggles colors and values dynamically for the line chart.

## Invariants & Coherence
- Taste & Minimal UI: Smooth bezier curve (`tension: 0.35`), subtle area fill, clean point markers, formatted ₹ tooltips, dark/light theme responsive colors.
- Neighboring charts (`categoriesChart`, `merchantsChart`) remain untouched and functional.

## Verification
- Run test suite: `pytest`
- Verify `app.js` syntax and chart rendering behavior.

